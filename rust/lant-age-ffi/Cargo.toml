[package]
name = "lant-age-ffi"
version = "1.0.0"
edition = "2021"
publish = false

[lib]
name = "lant_age_ffi"
crate-type = ["staticlib"]

[dependencies]
age = { version = "=0.12.1", default-features = false }
zeroize = "=1.8.2"

[profile.release]
lto = true
codegen-units = 1
panic = "unwind"
