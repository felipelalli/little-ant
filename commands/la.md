---
description: Little Ant — where should I focus now?
---

Run a Little Ant session: invoke the `little-ant` skill and follow it exactly.

User input: "$ARGUMENTS"

- Empty input → the standard session loop ("where should I focus now?").
- Otherwise interpret it through the skill's interaction grammar: a letter
  command, something to capture, or free text to classify — the skill decides.
