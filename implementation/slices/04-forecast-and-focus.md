# S04 — Forecast, hierarchy, and Focus

Status: **in progress**

## Outcome

Replace S02's singleton path with the complete subject-first, hierarchical,
replay-recorded weighted forecast, including Domain continuity, blockers,
parent descent, organization opportunities, Place continuation, and truthful
Focus consent.

## Canonical flow rows owned

- Useful non-pristine empty
- Focus proposal and active completion
- Cross-Domain focus
- N-step/branching blockers
- Project/collection descent
- Lazy claim review lottery
- Importance/forecast projections
- Required/preferred Place context

Generate every owning-row reference, the complete
[`forecast chapter`](../../spec/little-ant-1.0/05-focus-forecast-and-selection.md),
the complete
[`deterministic calculation profile`](../../spec/little-ant-1.0/deterministic-calculation-profile.md),
MOD-006, MOD-015, MOD-019..024, MOD-031..034, MOD-061,
WRK-035..037, WRK-157..160, and UX-F01..F14, UX-DM00..DM03,
UX-PL00..PL01, UX-LIST00.

## Work

1. Implement explicit eligibility gates before weighting and the closed
   opportunity catalog under FOC-037/FOC-054/FOC-061.
2. Implement one subject ticket, local opportunity selection, recursive parent
   descent, recurring-series hook points, and semantic leaf granularity.
3. Implement fixed-point weights, positive pressure correlation, Domain/family
   continuity, fatigue, age, positive tail, rejection sampling, and recorded
   provenance exactly from the profile.
4. Implement N-step and branching Dependency redirection, including typed
   non-Brick endpoints and full path inspection.
5. Implement direct non-exclusive Domain forest membership, effective path
   choice, soft continuity, `/domain-focus` one/stay/prefer scopes, and useful
   empty-scope recovery.
6. Implement required/preferred Place as post-draw continuation, not a hidden
   ticket or inferred geofence.
7. Implement Focus uncertainty, explanation from the immutable draw,
   transactional browsing from existing focus, and zero-eligible recovery.
8. Implement read-only importance and forecast views without consuming random
   blocks or exposing a public score.

## Gate

- every calculation reference vector and random-purpose isolation test passes;
- candidate ordering is canonical and every admitted candidate has positive
  chance;
- inspection and redraw of an envelope never consume another block;
- blockers redirect rather than suppress the originally drawn subject;
- hard Domain scope never silently falls through globally;
- parent selection descends to executable leaves and returns the parent only
  for its typed review;
- complete fixed-stream SCN-FOC and SCN-DOM replays match exact choices,
  weights, paths, reasons, and screens.
